# Per-server config (may contain host-specific paths / secrets)
config.env

# Local scheduling artifacts and captured output
*.log
*.err

# Python
__pycache__/
*.pyc
.venv/
venv/

# macOS
.DS_Store

# Dashboard build/deps
node_modules/
dist/
dashboard/backend/.venv/

# Resource-history sqlite (runtime data)
*.db
*.db-journal
*.db-wal
*.db-shm
dashboard/backend/resources.db
dashboard/backend/guardian_config.json

# local backup output (also lives outside the repo)
servermanager-backups/
dashboard/backend/pins.json
